package structure;

public abstract class TrailerMechanicalTransport extends RoadTransport{
    public TrailerMechanicalTransport(String engine, int countAxis, int countWheels) {
        super(engine, countAxis, countWheels);
    }
}
